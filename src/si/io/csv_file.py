from data import dataset
import pandas as pd

def read_csv(file_name: str, sep: str = ';', features: bool = True, label: bool = False) -> Dataset:
    #expected output - dataset object, reads specified file and returns Dataset object, use packages like "pandas"
    #para casa: varying threshold
    
def read_data_file(file_name: str, sep: str = ';', label: bool = False) -> Dataset:
    #use packages like numpy.genfromtxt
    #MAR, absolute score, implementar para tarefas de classificação